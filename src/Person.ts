export abstract class Person {
    private id: number;
    private firstName: string;
    private lastName: string;
    private tel : string;
    private email : string;

    constructor (id: number, firstName: string, lastName: string, tel: string, email: string){

    }
    
}